import { useCallback, useEffect, useState, type FormEvent } from 'react';
import { Link, Navigate, Route, Routes, useNavigate, useParams } from 'react-router-dom';
import type { PromptVariable, TemplateCategory } from '../types/prompt';
import { api } from '../lib/api';

type Admin={id:string;email:string;displayName:string;role:string};
type Template={id:string;name:string;summary:string;description:string;category:TemplateCategory;tags:string[];scenarios:string[];variables:PromptVariable[];promptTemplate:string;negativePrompt?:string;coverUrl?:string;status:string;source:string;updatedAt:string};
type Job={id:string;type:string;status:string;input:unknown;output:unknown;errorMessage?:string;createdAt:string};
type Provider={id:string;name:string;kind:string;protocol:string;baseUrl:string;apiKeyEnv?:string;apiKeyConfigured:boolean;defaultModel:string;defaults:Record<string,unknown>;enabled:boolean;isDefault:boolean};
type DraftForm={name:string;summary:string;description:string;category:TemplateCategory;tags:string[];scenarios:string[];variables:PromptVariable[];promptTemplate:string;negativePrompt:string;source:string};
const cats:TemplateCategory[]=['portrait','ecommerce','poster','logo','illustration','edit'];
const blank=():DraftForm=>({name:'',summary:'',description:'',category:'illustration',tags:[],scenarios:[],variables:[{id:'var-1',key:'subject',label:'主体',type:'text',required:true}],promptTemplate:'{{subject}}',negativePrompt:'',source:'manual'});
const field='w-full rounded-lg border border-gray-200 bg-white px-3 py-2 text-sm outline-none focus:border-violet-500 focus:ring-2 focus:ring-violet-100';
const button='rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium text-white hover:bg-violet-700 disabled:opacity-50';

export function AdminPage(){
  const [admin,setAdmin]=useState<Admin|null|undefined>(undefined);
  const refresh=useCallback(()=>api<Admin>('/api/admin/auth/me').then(setAdmin).catch(()=>setAdmin(null)),[]);
  useEffect(()=>{refresh()},[refresh]);
  if(admin===undefined)return <div className="grid min-h-screen place-items-center text-sm text-gray-500">正在验证会话…</div>;
  if(!admin)return <Login onLogin={setAdmin}/>;
  return <AdminShell admin={admin} onLogout={async()=>{await api('/api/admin/auth/logout',{method:'POST'});setAdmin(null)}}/>;
}

function Login({onLogin}:{onLogin:(a:Admin)=>void}){
  const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);
  async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{onLogin(await api<Admin>('/api/admin/auth/login',{method:'POST',body:JSON.stringify({email,password})}))}catch(x){setError(x instanceof Error?x.message:'登录失败')}finally{setBusy(false)}}
  return <div className="grid min-h-screen place-items-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-8 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-violet-600">Promptix Admin</p><h1 className="mt-3 text-2xl font-semibold">运营后台登录</h1><div className="mt-7 space-y-4"><input className={field} type="email" placeholder="邮箱" value={email} onChange={e=>setEmail(e.target.value)} required/><input className={field} type="password" placeholder="密码" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className={`${button} w-full`} disabled={busy}>{busy?'登录中…':'登录'}</button></div><Link to="/" className="mt-5 block text-center text-xs text-gray-400">返回前台</Link></form></div>
}

function AdminShell({admin,onLogout}:{admin:Admin;onLogout:()=>void}){
  return <div className="min-h-screen bg-gray-50 text-gray-900"><header className="border-b bg-slate-950 text-white"><div className="mx-auto flex max-w-[1500px] items-center gap-6 px-5 py-4"><Link to="/admin/templates" className="text-lg font-bold">Promptix <span className="text-violet-400">Admin</span></Link><nav className="flex flex-1 gap-1 text-sm"><Nav to="/admin/templates">模板</Nav><Nav to="/admin/ingest">智能入库</Nav><Nav to="/admin/jobs">任务</Nav><Nav to="/admin/providers">Providers</Nav></nav><span className="hidden text-xs text-gray-400 md:inline">{admin.email}</span><button onClick={onLogout} className="text-sm text-gray-300 hover:text-white">退出</button></div></header><main className="mx-auto max-w-[1500px] p-5 md:p-8"><Routes><Route index element={<Navigate to="templates" replace/>}/><Route path="templates" element={<TemplateList/>}/><Route path="templates/new" element={<TemplateEditor/>}/><Route path="templates/:id" element={<TemplateEditor/>}/><Route path="ingest" element={<Ingest/>}/><Route path="jobs" element={<Jobs/>}/><Route path="providers" element={<Providers/>}/><Route path="*" element={<Navigate to="templates" replace/>}/></Routes></main></div>
}
function Nav({to,children}:{to:string;children:string}){return <Link className="rounded-lg px-3 py-2 text-gray-300 hover:bg-white/10 hover:text-white" to={to}>{children}</Link>}
function Header({title,action}:{title:string;action?:React.ReactNode}){return <div className="mb-6 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-violet-600">Operations</p><h1 className="mt-1 text-2xl font-semibold">{title}</h1></div>{action}</div>}

function TemplateList(){
  const [items,setItems]=useState<Template[]>([]);const [q,setQ]=useState('');const [status,setStatus]=useState('');const [error,setError]=useState('');
  const load=useCallback(()=>api<Template[]>(`/api/admin/templates?${new URLSearchParams({...(q?{q}:{}),...(status?{status}:{})})}`).then(setItems).catch(e=>setError(e.message)),[q,status]);useEffect(()=>{load()},[load]);
  async function action(id:string,op:string){try{await api(`/api/admin/templates/${id}/${op}`,{method:'POST'});load()}catch(e){setError(e instanceof Error?e.message:'操作失败')}}
  return <><Header title="模板管理" action={<Link className={button} to="new">新建模板</Link>}/><div className="mb-4 flex gap-3"><input className={`${field} max-w-sm`} placeholder="搜索模板" value={q} onChange={e=>setQ(e.target.value)}/><select className={`${field} max-w-36`} value={status} onChange={e=>setStatus(e.target.value)}><option value="">全部状态</option><option value="draft">草稿</option><option value="published">已发布</option><option value="archived">已下架</option></select></div>{error&&<Notice text={error}/>}<div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs text-gray-500"><tr><th className="p-3">模板</th><th>分类</th><th>状态</th><th>更新时间</th><th className="p-3 text-right">操作</th></tr></thead><tbody>{items.map(t=><tr key={t.id} className="border-t"><td className="p-3"><Link className="font-medium hover:text-violet-600" to={t.id}>{t.name}</Link><p className="mt-1 max-w-xl truncate text-xs text-gray-400">{t.summary}</p></td><td>{t.category}</td><td><Status value={t.status}/></td><td className="text-xs text-gray-500">{new Date(t.updatedAt).toLocaleString()}</td><td className="p-3 text-right"><Link className="mr-3 text-violet-600" to={t.id}>编辑</Link>{t.status!=='published'?<button onClick={()=>action(t.id,'publish')} className="text-emerald-600">发布</button>:<button onClick={()=>action(t.id,'archive')} className="text-amber-600">下架</button>}</td></tr>)}</tbody></table>{!items.length&&<p className="p-10 text-center text-sm text-gray-400">暂无模板</p>}</div></>
}

function TemplateEditor(){
  const {id}=useParams();const navigate=useNavigate();const [form,setForm]=useState(blank());const [existing,setExisting]=useState<Template|null>(null);const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const [genJob,setGenJob]=useState<Job|null>(null);
  useEffect(()=>{if(id)api<Template>(`/api/admin/templates/${id}`).then(t=>{setExisting(t);setForm({...blank(),...t,tags:t.tags??[],scenarios:t.scenarios??[],variables:t.variables??[],negativePrompt:t.negativePrompt??''})}).catch(e=>setMessage(e.message))},[id]);
  const set=(key:string,value:unknown)=>setForm(f=>({...f,[key]:value}));
  async function save(){setBusy(true);setMessage('');try{const payload={...form,tags:form.tags,scenarios:form.scenarios};const t=await api<Template>(id?`/api/admin/templates/${id}`:'/api/admin/templates',{method:id?'PATCH':'POST',body:JSON.stringify(payload)});setExisting(t);setMessage('已保存');if(!id)navigate(`/admin/templates/${t.id}`,{replace:true})}catch(e){setMessage(e instanceof Error?e.message:'保存失败')}finally{setBusy(false)}}
  async function upload(file:File){if(!id){setMessage('请先保存模板，再上传封面');return}const body=new FormData();body.set('file',file);try{const t=await api<Template>(`/api/admin/templates/${id}/cover`,{method:'POST',body});setExisting(t);setMessage('封面已上传')}catch(e){setMessage(e instanceof Error?e.message:'上传失败')}}
  async function publish(){if(!id)return;try{const t=await api<Template>(`/api/admin/templates/${id}/publish`,{method:'POST'});setExisting(t);setMessage('发布成功')}catch(e){setMessage(e instanceof Error?e.message:'发布失败')}}
  async function generate(){if(!id)return;try{const x=await api<{jobId:string}>(`/api/admin/jobs`,{method:'POST',body:JSON.stringify({type:'image_generate',templateId:id,input:{prompt:form.promptTemplate,n:1}})});setMessage('生图任务已提交');poll(x.jobId)}catch(e){setMessage(e instanceof Error?e.message:'提交失败')}}
  function poll(jobId:string){const timer=window.setInterval(async()=>{const j=await api<Job>(`/api/admin/jobs/${jobId}`);setGenJob(j);if(['succeeded','failed'].includes(j.status))window.clearInterval(timer)},1500)}
  async function setGeneratedCover(){if(!id||!genJob)return;const t=await api<Template>(`/api/admin/jobs/${genJob.id}/set-cover`,{method:'POST',body:JSON.stringify({templateId:id,imageIndex:0})});setExisting(t);setMessage('生成图已设为封面')}
  function updateVar(i:number,key:string,value:unknown){set('variables',form.variables.map((v,n)=>n===i?{...v,[key]:value}:v))}
  return <><Header title={id?'编辑模板':'新建模板'} action={<div className="flex gap-2"><Link className="rounded-lg border px-4 py-2 text-sm" to="/admin/templates">返回</Link><button className={button} onClick={save} disabled={busy}>{busy?'保存中…':'保存草稿'}</button>{id&&<button className="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white" onClick={publish}>发布</button>}</div>}/>{message&&<Notice text={message}/>}<div className="grid gap-6 lg:grid-cols-[1fr_340px]"><section className="space-y-5 rounded-xl border bg-white p-5"><h2 className="font-semibold">基础信息</h2><div className="grid gap-4 md:grid-cols-2"><Input label="名称" value={form.name} onChange={v=>set('name',v)}/><label className="text-sm">分类<select className={`${field} mt-1`} value={form.category} onChange={e=>set('category',e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select></label></div><Input label="摘要" value={form.summary} onChange={v=>set('summary',v)}/><Area label="描述" value={form.description} onChange={v=>set('description',v)}/><Input label="标签（逗号分隔）" value={form.tags.join(',')} onChange={v=>set('tags',v.split(',').map(x=>x.trim()).filter(Boolean))}/><Input label="适用场景（逗号分隔）" value={form.scenarios.join(',')} onChange={v=>set('scenarios',v.split(',').map(x=>x.trim()).filter(Boolean))}/><div className="flex items-center justify-between"><h2 className="font-semibold">变量</h2><button className="text-sm text-violet-600" onClick={()=>set('variables',[...form.variables,{id:`var-${Date.now()}`,key:'variable',label:'变量',type:'text'}])}>+ 添加变量</button></div>{form.variables.map((v,i)=><div key={v.id} className="grid gap-2 rounded-lg bg-gray-50 p-3 md:grid-cols-[1fr_1fr_130px_auto]"><input className={field} value={v.key} onChange={e=>updateVar(i,'key',e.target.value)} placeholder="key"/><input className={field} value={v.label} onChange={e=>updateVar(i,'label',e.target.value)} placeholder="标签"/><select className={field} value={v.type} onChange={e=>updateVar(i,'type',e.target.value)}>{['text','select','number','ratio','image'].map(x=><option key={x}>{x}</option>)}</select><button className="px-2 text-red-500" onClick={()=>set('variables',form.variables.filter((_,n)=>n!==i))}>删除</button></div>)}<Area label="Prompt 骨架" value={form.promptTemplate} onChange={v=>set('promptTemplate',v)} rows={8}/><Area label="负面提示词" value={form.negativePrompt} onChange={v=>set('negativePrompt',v)} rows={3}/></section><aside className="space-y-5"><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">封面与状态</h2>{existing?.coverUrl?<img className="mt-4 aspect-[4/3] w-full rounded-lg object-cover" src={existing.coverUrl}/>:<div className="mt-4 grid aspect-[4/3] place-items-center rounded-lg bg-gray-100 text-sm text-gray-400">暂无封面</div>}<label className="mt-3 block cursor-pointer rounded-lg border border-dashed p-3 text-center text-sm text-violet-600">上传封面<input hidden type="file" accept="image/*" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/></label><p className="mt-3 text-sm">状态：<Status value={existing?.status??'draft'}/></p></div><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">AI 生成封面</h2><p className="mt-2 text-xs leading-5 text-gray-500">按当前 Prompt 骨架提交异步生图任务。</p><button className={`${button} mt-3 w-full`} onClick={generate} disabled={!id}>生成封面</button>{genJob&&<div className="mt-3 text-sm"><Status value={genJob.status}/>{genJob.status==='succeeded'&&<button className="ml-3 text-violet-600" onClick={setGeneratedCover}>设为封面</button>}{genJob.errorMessage&&<p className="mt-2 text-xs text-red-600">{genJob.errorMessage}</p>}</div>}</div></aside></div></>
}

function Ingest(){
  const [text,setText]=useState('');const [file,setFile]=useState<File>();const [job,setJob]=useState<Job>();const [message,setMessage]=useState('');const [providerItems,setProviderItems]=useState<Provider[]>([]);const [providerId,setProviderId]=useState('');const navigate=useNavigate();
  useEffect(()=>{api<Provider[]>('/api/admin/providers').then(list=>{const eligible=list.filter(p=>p.enabled&&(p.kind==='llm'||p.kind==='both'));setProviderItems(eligible);const preferred=eligible.find(p=>p.isDefault)??eligible[0];if(preferred)setProviderId(preferred.id)}).catch(e=>setMessage(e instanceof Error?e.message:'Provider 加载失败'))},[]);
  async function submit(){try{let x:{jobId:string};if(file){const body=new FormData();body.set('file',file);if(providerId)body.set('providerId',providerId);x=await api('/api/admin/jobs/image-reverse',{method:'POST',body})}else{x=await api('/api/admin/jobs',{method:'POST',body:JSON.stringify({type:'text_expand',input:{text},providerId:providerId||undefined})})}poll(x.jobId)}catch(e){setMessage(e instanceof Error?e.message:'提交失败')}}
  function poll(id:string){const timer=window.setInterval(async()=>{const j=await api<Job>(`/api/admin/jobs/${id}`);setJob(j);if(['succeeded','failed'].includes(j.status))window.clearInterval(timer)},1500)}
  async function save(){if(!job?.output)return;try{const t=await api<Template>('/api/admin/templates',{method:'POST',body:JSON.stringify({...job.output as object,source:job.type})});navigate(`/admin/templates/${t.id}`)}catch(e){setMessage(e instanceof Error?e.message:'保存失败')}}
  return <><Header title="智能入库"/><div className="grid gap-6 md:grid-cols-2"><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">提示词优化 / 图片反推</h2><label className="mt-4 block text-sm">优化与结构化模型<select className={`${field} mt-1`} value={providerId} onChange={e=>setProviderId(e.target.value)}><option value="">使用默认 Provider</option>{providerItems.map(p=><option key={p.id} value={p.id}>{p.name} · {p.defaultModel}</option>)}</select></label><p className="mt-2 text-xs leading-5 text-gray-500">选择 DeepSeek 时，文本会直接优化；图片会先经过已标记“支持图片理解”的视觉 Provider，再由 DeepSeek 结构化。</p><textarea className={`${field} mt-4 min-h-44`} value={text} onChange={e=>{setText(e.target.value);if(e.target.value)setFile(undefined)}} placeholder="粘贴现有提示词或创意需求，DeepSeek 将进行扩写、优化和结构化…"/><p className="my-4 text-center text-xs text-gray-400">或</p><label className="block rounded-lg border border-dashed p-6 text-center text-sm text-gray-500">{file?file.name:'上传参考图进行反推'}<input hidden type="file" accept="image/*" onChange={e=>{setFile(e.target.files?.[0]);if(e.target.files?.[0])setText('')}}/></label><button className={`${button} mt-4 w-full`} disabled={(!text&&!file)||!providerId} onClick={submit}>提交异步任务</button>{message&&<p className="mt-3 text-sm text-red-600">{message}</p>}</section><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">任务结果</h2>{!job?<p className="mt-12 text-center text-sm text-gray-400">提交后结果会显示在这里</p>:<div className="mt-4"><Status value={job.status}/>{job.errorMessage&&<p className="mt-3 text-sm text-red-600">{job.errorMessage}</p>}{Boolean(job.output)&&<pre className="mt-4 max-h-[440px] overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-slate-200">{JSON.stringify(job.output,null,2)}</pre>}{job.status==='succeeded'&&<button className={`${button} mt-4`} onClick={save}>保存为草稿模板</button>}</div>}</section></div></>
}

function Jobs(){const [items,setItems]=useState<Job[]>([]);const [error,setError]=useState('');const load=useCallback(()=>api<Job[]>('/api/admin/jobs').then(setItems).catch(e=>setError(e.message)),[]);useEffect(()=>{load();const t=setInterval(load,5000);return()=>clearInterval(t)},[load]);async function retry(id:string){try{await api(`/api/admin/jobs/${id}/retry`,{method:'POST'});load()}catch(e){setError(e instanceof Error?e.message:'重试失败')}}return <><Header title="任务中心"/>{error&&<Notice text={error}/>}<div className="space-y-3">{items.map(j=><div key={j.id} className="flex items-center gap-4 rounded-xl border bg-white p-4"><div className="min-w-0 flex-1"><div className="flex gap-2"><b className="text-sm">{j.type}</b><Status value={j.status}/></div><p className="mt-1 truncate font-mono text-xs text-gray-400">{j.id}</p>{j.errorMessage&&<p className="mt-2 text-xs text-red-600">{j.errorMessage}</p>}</div><time className="hidden text-xs text-gray-400 sm:block">{new Date(j.createdAt).toLocaleString()}</time>{j.status==='failed'&&<button className="text-sm text-violet-600" onClick={()=>retry(j.id)}>重试</button>}</div>)}</div></>}

function Providers(){const [items,setItems]=useState<Provider[]>([]);const [form,setForm]=useState({name:'',kind:'llm',protocol:'openai_chat',baseUrl:'https://api.openai.com/v1',apiKeyEnv:'LLM_API_KEY',defaultModel:'gpt-4.1-mini',defaults:{supportsVision:false,size:'1024x1024',quality:'auto',response_format:'url',asyncPollIntervalMs:2000,asyncTimeoutMs:900000,maxQueueSeconds:120} as Record<string,unknown>,enabled:true,isDefault:true});const [message,setMessage]=useState('');const load=useCallback(()=>api<Provider[]>('/api/admin/providers').then(setItems),[]);useEffect(()=>{load()},[load]);function preset65535(){setForm({name:'65535 gpt-image-2',kind:'image',protocol:'openai_images_async',baseUrl:'https://img-cn.65535.space/v1',apiKeyEnv:'IMAGE_65535_API_KEY',defaultModel:'gpt-image-2',defaults:{supportsVision:false,size:'2048x2048',quality:'high',response_format:'url',asyncPollIntervalMs:2000,asyncTimeoutMs:900000,maxQueueSeconds:120},enabled:true,isDefault:true});setMessage('已载入 65535.space 推荐配置，请确认密钥环境变量后保存')}function presetDeepSeek(){setForm({name:'DeepSeek Prompt Optimizer',kind:'llm',protocol:'deepseek_chat',baseUrl:'https://api.deepseek.com',apiKeyEnv:'DEEPSEEK_API_KEY',defaultModel:'deepseek-v4-flash',defaults:{supportsVision:false,thinking:{type:'disabled'},max_tokens:4096},enabled:true,isDefault:true});setMessage('已载入 DeepSeek 官方配置。DeepSeek 不直接读取图片；图片反推还需一个勾选“支持图片理解”的 OpenAI Chat Provider')}async function save(){try{await api('/api/admin/providers',{method:'POST',body:JSON.stringify(form)});setMessage('Provider 已保存');load()}catch(e){setMessage(e instanceof Error?e.message:'保存失败')}}const setDefault=(key:string,value:unknown)=>setForm(f=>({...f,defaults:{...f.defaults,[key]:value}}));return <><Header title="Provider 配置" action={<div className="flex gap-2"><button className="rounded-lg border border-blue-200 bg-blue-50 px-4 py-2 text-sm font-medium text-blue-700" onClick={presetDeepSeek}>载入 DeepSeek 预设</button><button className="rounded-lg border border-violet-200 bg-violet-50 px-4 py-2 text-sm font-medium text-violet-700" onClick={preset65535}>载入 65535 生图预设</button></div>}/><div className="grid gap-6 lg:grid-cols-[460px_1fr]"><section className="space-y-4 rounded-xl border bg-white p-5"><Input label="名称" value={form.name} onChange={v=>setForm(f=>({...f,name:v}))}/><div className="grid grid-cols-2 gap-3"><label className="text-sm">类型<select className={`${field} mt-1`} value={form.kind} onChange={e=>setForm(f=>({...f,kind:e.target.value}))}><option>llm</option><option>image</option><option>both</option></select></label><label className="text-sm">协议<select className={`${field} mt-1`} value={form.protocol} onChange={e=>setForm(f=>({...f,protocol:e.target.value}))}><option value="openai_chat">OpenAI Chat</option><option value="deepseek_chat">DeepSeek Chat</option><option value="openai_images">OpenAI Images（同步）</option><option value="openai_images_async">OpenAI Images（异步轮询）</option></select></label></div><Input label="Base URL" value={form.baseUrl} onChange={v=>setForm(f=>({...f,baseUrl:v}))}/><Input label="密钥环境变量名" value={form.apiKeyEnv} onChange={v=>setForm(f=>({...f,apiKeyEnv:v}))}/><Input label="默认模型" value={form.defaultModel} onChange={v=>setForm(f=>({...f,defaultModel:v}))}/>{form.kind!=='image'&&form.protocol!=='deepseek_chat'&&<label className="flex items-center gap-2 rounded-lg bg-blue-50 p-3 text-sm text-blue-800"><input type="checkbox" checked={Boolean(form.defaults.supportsVision)} onChange={e=>setDefault('supportsVision',e.target.checked)}/>支持图片理解，可作为 DeepSeek 图片反推的第一阶段</label>}{form.kind!=='llm'&&<div className="rounded-lg bg-gray-50 p-3"><p className="mb-3 text-xs font-semibold uppercase tracking-wider text-gray-500">生图默认参数</p><div className="grid grid-cols-2 gap-3"><Input label="尺寸" value={String(form.defaults.size??'')} onChange={v=>setDefault('size',v)}/><label className="text-sm">质量<select className={`${field} mt-1`} value={String(form.defaults.quality??'auto')} onChange={e=>setDefault('quality',e.target.value)}><option>auto</option><option>low</option><option>medium</option><option>high</option></select></label><Input label="轮询间隔（毫秒）" value={String(form.defaults.asyncPollIntervalMs??2000)} onChange={v=>setDefault('asyncPollIntervalMs',Number(v))}/><Input label="最长等待（秒）" value={String(Number(form.defaults.asyncTimeoutMs??900000)/1000)} onChange={v=>setDefault('asyncTimeoutMs',Number(v)*1000)}/></div></div>}<button className={`${button} w-full`} onClick={save}>保存 Provider</button>{message&&<p className="text-sm text-gray-600">{message}</p>}</section><section className="space-y-3">{items.map(p=><div key={p.id} className="rounded-xl border bg-white p-4"><div className="flex items-center justify-between"><b>{p.name}</b><div className="flex gap-2"><Status value={p.enabled?'enabled':'disabled'}/>{p.isDefault&&<Status value="default"/>}</div></div><p className="mt-2 text-sm text-gray-500">{p.protocol} · {p.defaultModel}</p><p className="mt-1 truncate text-xs text-gray-400">{p.baseUrl} · {p.apiKeyEnv} {p.apiKeyConfigured?'✓':'未配置'}</p>{Boolean(p.defaults?.supportsVision)&&<p className="mt-2 text-xs font-medium text-blue-600">✓ 支持图片理解</p>}{p.defaults&&p.kind!=='llm'&&<p className="mt-2 text-xs text-gray-400">默认：{String(p.defaults.size??'—')} · {String(p.defaults.quality??'auto')}</p>}</div>)}</section></div></>}

function Input({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block text-sm">{label}<input className={`${field} mt-1`} value={value} onChange={e=>onChange(e.target.value)}/></label>}
function Area({label,value,onChange,rows=4}:{label:string;value:string;onChange:(v:string)=>void;rows?:number}){return <label className="block text-sm">{label}<textarea rows={rows} className={`${field} mt-1`} value={value} onChange={e=>onChange(e.target.value)}/></label>}
function Notice({text}:{text:string}){return <div className={`mb-4 rounded-lg border px-4 py-3 text-sm ${text.includes('成功')||text.includes('已')?'border-emerald-200 bg-emerald-50 text-emerald-700':'border-amber-200 bg-amber-50 text-amber-800'}`}>{text}</div>}
function Status({value}:{value:string}){const good=['published','succeeded','enabled','default'].includes(value),bad=['failed','cancelled'].includes(value);return <span className={`inline-flex rounded-full px-2 py-0.5 text-xs font-medium ${good?'bg-emerald-100 text-emerald-700':bad?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700'}`}>{value}</span>}
