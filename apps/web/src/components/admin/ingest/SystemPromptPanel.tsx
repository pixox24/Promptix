import { useState } from 'react'; import { api } from '../../../lib/api'; import type { IngestFlowType } from '@promptix/shared'; import type { IngestPromptConfig } from '../../../types/ingest';
export function SystemPromptPanel({flowType,config,onChange}:{flowType:IngestFlowType;config:IngestPromptConfig;onChange:(c:IngestPromptConfig)=>void}){const [value,setValue]=useState(config.prompt);const [message,setMessage]=useState('');async function save(global:boolean){try{if(global){const c=await api<IngestPromptConfig>(`/api/admin/ingest/system-prompts/${flowType}`,{method:'PUT',body:JSON.stringify({prompt:value})});onChange(c);setMessage('已保存为系统提示词')}else setMessage('仅本次修改');}catch(e){setMessage(e instanceof Error?e.message:'保存失败')}}return <div className="rounded-xl border bg-white p-4"><div className="flex items-center justify-between"><h3 className="font-semibold">系统提示词</h3><button type="button" className="text-xs text-violet-600" onClick={()=>{setValue(config.prompt);setMessage('已恢复全局预设')}}>恢复全局预设</button></div><textarea className="mt-3 min-h-32 w-full rounded-lg border p-3 text-sm" value={value} onChange={e=>setValue(e.target.value)}/><div className="mt-3 flex items-center gap-3"><button type="button" className="rounded-lg bg-violet-600 px-3 py-2 text-sm text-white" onClick={()=>save(true)}>保存为系统提示词</button><button type="button" className="rounded-lg border px-3 py-2 text-sm" onClick={()=>save(false)}>仅本次修改</button>{message&&<span className="text-xs text-gray-500">{message}</span>}</div></div>}
