import { Trash2 } from 'lucide-react';
import type { SavedDraft } from '../../types/prompt';
export function DraftLocker({ drafts, activeId, onLoad, onDelete }: { drafts:SavedDraft[]; activeId:string|null; onLoad:(draft:SavedDraft)=>void; onDelete:(id:string)=>void }) { if(!drafts.length)return null; return <section><h2 className="mb-2 text-xs font-semibold text-slate-500">草稿备忘箱</h2><div className="max-h-44 space-y-2 overflow-y-auto pr-1">{drafts.map(draft=><div key={draft.id} className={`flex items-center gap-2 rounded-md border p-2 ${activeId===draft.id?'border-slate-700':'border-slate-200'}`}><button type="button" onClick={()=>onLoad(draft)} className="min-w-0 flex-1 text-left"><p className="truncate text-xs text-slate-700">{draft.prompt}</p><p className="mt-1 text-[10px] text-slate-400">{new Date(draft.updatedAt).toLocaleString('zh-CN')}{draft.aspectRatio?` · ${draft.aspectRatio}`:''}</p></button><button type="button" onClick={()=>onDelete(draft.id)} aria-label="删除草稿" className="grid h-8 w-8 place-items-center text-slate-400 hover:text-red-600"><Trash2 size={14}/></button></div>)}</div></section> }
